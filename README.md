# SQL Export CLI

### Purpose

* CLI tool to perform queries against a database using a JDBC driver
* Ability to export results to the console or a file in different formats (such as a table text format, csv, json, etc.)
* Ability to run one command to output

### TODO

* Add ability to pipe commands
* Add ability to stream to files (> or >>)
* Add ability to run not in CLI mode - execute command string
* Add yml configuration file

### Licensing 

Please see the file called [LICENSE](LICENSE)